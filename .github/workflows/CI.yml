name: CI

on:
  push:
    branches: [**]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2.3.1
    - uses: actions/setup-node@v2.0.0
    - run: npm install
    - run: 
    - run: npm run lint
